# Deferred Items

These refactor targets are intentionally deferred from the current implementation pass.

- `angee dev` does not start the in-process HTTP/MCP operator for the lifetime of the dev command.
- Embedded fallback templates are not implemented.
